export enum StatusJob {
    NEW = "new",
    PENDING = "pending",
    WAITING = "waiting",
    RUNNING = "running",
    SUCCESSFUL = "successful",
    FAILED = "failed",
    ERROR = "error",
    CANCELED = "canceled",
    NEVER_UPDATED = "never updated"
}

