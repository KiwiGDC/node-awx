import {AnsibleApi} from "../src";

// TODO: ADD AWX GENERIC INSTANCE FOR TESTS INTO GIT OR SOMETHING ELSE (DOCKER?)
/**test('get inventory', async () => {
    expect(AnsibleApi.GETINSTANCE().url.toString()).toBeDefined()

});**/

